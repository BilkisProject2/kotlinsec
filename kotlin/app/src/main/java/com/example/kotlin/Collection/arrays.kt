package com.example.kotlin.Collection


//list is dynamic
//we can ad remove
/*
array have fix size but we can update size
but in imutable we not do this also
1 > mutable list = can be chaned element can be add remove and replace
2> immutable list(cant be changed)
 */

fun main()
{
    val nums = listOf(1,2,3)
    println(nums.lastIndex)
    println(nums.first())
}

class arrays {
}